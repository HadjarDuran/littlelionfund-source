{
  "name": "lion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:ci": "bash scripts/ci-deploy.sh"
  },
  "dependencies": {
    "@upstash/redis": "^1.31.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
